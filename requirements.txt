# Dépendances du système d'acquisition B2B (CLAUDE.md)
# Liste volontairement minimale : on ajoute une dépendance seulement
# quand un module en cours de construction en a réellement besoin.

# Appels à l'API Claude (génération de messages, scoring, signaux)
anthropic==0.40.0

# Lecture/écriture et manipulation des CSV de prospects
pandas==2.2.3

# Chargement des clés API depuis le fichier .env
python-dotenv==1.0.1

# Appels web pour l'enrichissement (module 1), la détection de signaux
# (module 3) et le moteur IA gratuit Ollama en local
requests==2.32.3
httpx==0.27.2

# Interface web locale (interface_web/), qui pilote les modules par des
# boutons plutôt que des commandes
flask==3.0.3

# Note : sqlite3 (module 5, CRM) fait partie de la bibliothèque standard
# Python, donc pas de dépendance à ajouter ici.
